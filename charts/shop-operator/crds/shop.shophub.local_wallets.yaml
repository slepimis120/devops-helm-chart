---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: wallets.shop.shophub.local
spec:
  group: shop.shophub.local
  names:
    kind: Wallet
    listKind: WalletList
    plural: wallets
    singular: wallet
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.network
      name: Network
      type: string
    - jsonPath: .status.address
      name: Address
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              network:
                default: ethereum
                description: |-
                  Blockchain network. EVM networks (ethereum, polygon) share the same
                  secp256k1/keccak address format; bitcoin is not supported for generation yet.
                enum:
                - ethereum
                - polygon
                - bitcoin
                type: string
            type: object
          status:
            properties:
              address:
                description: Generated public wallet address.
                type: string
              balance:
                description: Balance (placeholder until an on-chain lookup is wired
                  in).
                type: string
              phase:
                description: 'Phase: "Active" (keypair generated) or "Unsupported"
                  (network).'
                type: string
              secretRef:
                description: Name of the Secret holding the private key (`<name>-wallet`).
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
