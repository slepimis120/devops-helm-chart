---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: discordchannels.shop.shophub.local
spec:
  group: shop.shophub.local
  names:
    kind: DiscordChannel
    listKind: DiscordChannelList
    plural: discordchannels
    singular: discordchannel
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              botTokenSecretRef:
                description: Discord bot token (stored as secret reference)
                type: string
              channelName:
                description: Channel name
                type: string
              serverId:
                description: Discord server ID
                type: string
            required:
            - channelName
            - serverId
            type: object
          status:
            properties:
              channelId:
                description: Discord channel ID
                type: string
              status:
                description: 'Status: "Configured", "Failed"'
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
